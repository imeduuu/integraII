/**
 * Vista placeholder para panel administrativo
 */
import React from 'react';

/**
 * Componente temporal para el dashboard administrativo
 * Será reemplazado por panel de control completo con métricas y gestión
 */
const AdminView: React.FC = () => (
  <div style={{ padding: '2rem', textAlign: 'center' }}>
    <h2 style={{ color: '#2563eb', fontWeight: 700, fontSize: '2rem' }}>
      Vista Administrador (AdminView)
    </h2>
    <p>Componente provisional para el rol administrador.</p>
  </div>
);

export default AdminView;